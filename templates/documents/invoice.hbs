<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Invoice</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
    }

    h1 {
      color: #4CAF50;
    }

    .invoice-box {
      max-width: 800px;
      margin: auto;
      border: 1px solid #eee;
      padding: 30px;
      line-height: 24px;
      font-size: 14px;
    }

    .table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
    }

    .table th,
    .table td {
      border: 1px solid #ddd;
      padding: 8px;
    }

    .total {
      font-weight: bold;
      text-align: right;
    }
  </style>
</head>

<body>
  <div class="invoice-box">
    <h1>Invoice</h1>
    <p><strong>Invoice Number:</strong> {{invoiceNumber}}</p>
    <p><strong>Date:</strong> {{issueDate}}</p>
    <p><strong>Issuer:</strong> {{issuer.name}} ({{issuer.taxId}})</p>
    <p><strong>Receiver:</strong> {{receiver.name}} ({{receiver.taxId}})</p>

    <table class="table">
      <thead>
        <tr>
          <th>Description</th>
          <th>Qty</th>
          <th>Unit Price</th>
          <th>Tax Rate</th>
          <th>Total</th>
        </tr>
      </thead>
      <tbody>
        {{#each items}}
        <tr>
          <td>{{description}}</td>
          <td>{{quantity}}</td>
          <td>{{unitPrice}}</td>
          <td>{{taxRate}}%</td>
          <td>{{total}}</td>
        </tr>
        {{/each}}
      </tbody>
    </table>

    <p class="total">Total Sales: {{totalSales}}</p>
    <p class="total">Total Tax: {{totalTax}}</p>
    <p class="total">Total Amount: {{totalAmount}}</p>
  </div>
</body>

</html>